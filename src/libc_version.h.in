// libc_version.h

#ifndef __LIBC_VERSION_H__
#define __LIBC_VERSION_H__

#define LIBC_VERSION_MAJOR 0
#define LIBC_VERSION_MINOR 9
#define LIBC_VERSION_BUILD 0

/*
	svn revision: $WCREV$
	svn revision date: $WCDATE$
	working copy URL: $WCURL$
*/

// DO NOT MODIFY BELOW THIS LINE

//#define _MAKE_VERSION_STING(M,m,b) #M "." #m "." #b
//#define STRINGIFY(M) #M
//#define _MAKE_VERSION_STING(M,m,b) STRINGIFY(M##.##m##.##b)
//#define MAKE_VERSION_STING(M,m,b) _MAKE_VERSION_STING(M,m,b)
//#define MAKE_VERSION_INT(M,m,b) (((M) << 16) | ((m) << 8) | (b))

#define _LIBC_VERSION_STRING(M,m,b) #M "." #m "." #b
#define LIBC_VERSION_STRING _LIBC_VERSION_STRING(LIBC_VERSION_MAJOR,LIBC_VERSION_MINOR,LIBC_VERSION_BUILD)
#define LIBC_FANCY_VERSION_STRING "libc-" LIBC_VERSION_STRING "-$WCREV$"
#define LIBC_VERSION_INT (100 * 100 * LIBC_VERSION_MAJOR + 100 * LIBC_VERSION_MINOR + LIBC_VERSION_BUILD)

#define LIBC_SVN_REVISION_STRING "$WCREV$"
#define LIBC_SVN_REVISION_INT $WCREV$

#endif // __LIBC_VERSION_H__
